# 独立质量审查报告 · 需求提交入口增量改造

审查官：严过审（critique-reviewer）｜性质：**独立审查方，不参与制作**
审查基准：`outputs/02a-demand-entry-design-spec.md`（18 条红线）· `03-product-solution.md`（决策 3 + 硬判据）· `04-delivery-notes.md`（第 6 节工程教训）
审查方式：**读码 + 真跑产物**（vite build / jsdom smoke / headless Chrome CDP 直连 `file://` / 变异测试）。构建师汇报的每个"已完成"均已独立复核。

---

## 1. 总体结论：**REVISE**（总分 19 / 25）

设计与规则引擎的主体质量很高：18 条红线通过 17 条，5 个评分维度全部实测为真、且是真正的分数档（非整数跳变），语义色边界守得住。
但存在 **1 个 P0 布局缺陷（移动端提交页区块顺序错乱）** 和 **1 个 P0 级测试缺陷（一条断言被证明是空转的）**，均属本项目明确列为"不可接受"的两类问题，故不能放行。

| 维度 | 分 | 说明 |
|---|---|---|
| 设计哲学 | **5** | 受众分流（轻量/BRD）真正落地；「完整度不是分数」「agent 是协作者不是评分官」贯穿数据、文案、交互三层，且每个决定都有交代。 |
| 信息层次 | **4** | 首页区块层级完全按裁决落地（含 4px 唯一强调条）；提交页左右分栏成立。**扣分**：≤900 单列时表单被排到公开列表之后（P0），单列层级不成立。 |
| 执行完成度 | **3** | 规格主体落地、边界情况（说不清/选填/切档不丢数据）处理正确；但**新增 CSS 的两条**里 `display:contents` 因内联样式失效（未验）、模板数据 `brdTemplates` 是死数据、smoke 有 1 条空转断言。 |
| 特异性 | **5** | 问题-方案分离、说不清定位、量级三胶囊、验收标准选填——全是针对"类 A 业务团队 + 类 B 工程师"真实形态设计，不是通用模板。 |
| 克制 | **2** | **扣分项**：新增了 2 组 `@media ≤900px` CSS（超出 spec "唯一允许 1 条"）；`.dp-demand-side` 上加内联 `display:flex` 与同组 CSS 的 `display:contents` 自相矛盾（同一意图两处表达，且一处无效）；`brdTemplates`/`demandTracks` 死数据；`dp-demand-promise` 类被复用在两个语义不同的卡上。 |

**总分 19 / 25。任一维度 < 3 即 REVISE，本报告「克制」维度 2 分 + 存在 P0。**

---

## 2. 18 条红线逐条核对（通过 / 违反 + 证据）

| # | 红线 | 判定 | 证据（实测） |
|---|---|---|---|
| 1 | 不给大卡铺品牌蓝实底 | ✅通过 | 大卡为白底 `.dp-card` + 品牌蓝 4px 左边条；无 `background:brand` 面积色 |
| 2 | 强调条不超 4px 且**全页唯一** | ✅通过 | 实测 `#/home` 4px 绝对定位条 **恰好 1 处**（大卡），3px **2 处**（置顶决议）；其余 5 个路由均 0；变异 M1 把 4px→8px，断言立即变红 |
| 3 | 入口区块禁用 YellowMark | ✅通过 | 入口区块 DOM 内无 `.dp-yellow` / `YellowMark` |
| 4 | 红黄绿不得表达完整度 | ✅通过 | 完整度卡片内语义色命中 **0 处**（`SEM_IN_COMPLETENESS_BLOCK hitCount=0`）；页面唯一的 success 色在「已完成」**状态胶囊**（状态非评价，允许） |
| 5 | 需求计数不得用 warning/error Pill | ✅通过 | 三计数为 `.dp-num` 28px + `.dp-ink` 中性数字 + 中性 StatusDot，无 Pill 无语义色 |
| 6 | 表单必须挂 `dp-form` | ✅通过 | `<div className="dp-form">` 存在；smoke 断言通过 |
| 7 | 实时校验提示必须中性 | ✅通过 | 未提交时提示 `color:c.text3`；仅点击提交被拦后出现 `c.error`（spec §4 明确允许）。实测：点提交后 5 条 `rgb(227,38,47)` 提示 + `还差 5 项必填，补上就能提交` |
| 8 | agent 不得一次列 5 项 | ✅通过 | init 气泡为门槛降低语；建议类气泡**恒为 1 条**（`ADVICE_BUBBLE_COUNT=1`） |
| 9 | 评分不得用整数档跳变 | ✅通过 | 实测 D2 三档 = 7% / 13% / 20%；D3「说不清」= +20 的一半权重（"说到一半"）；`pct` 可为 47%/67% 等任意值 |
| 10 | 不得新增第六顶栏项 | ✅通过 | 六档实测 `navs=5`（恒定）；smoke 断言 5 项通过 |
| 11 | 公开列表不得折叠隐藏 | ✅通过 | 无 `.ant-collapse`；smoke 断言通过；375 下列表 `display:block, h=712` 正常展开 |
| 12 | 不得给 Panel 加阴影 | ✅通过 | 新增卡均 `.dp-card` 静止无阴影；无 `shadowFloating` |
| 13 | 不得新增 CSS 类与色值 | ⚠️**有争议** | 新增 **2 组** `@media ≤900px` 规则（`.dp-demand-entry-body` padding 组 + `.dp-demand-side display:contents` + 4 条 order）。spec §13 只允许 **1 条**（焦点环）。裁决见第 7 节 |
| 14 | `#B3B7B9` 不得做任何正文 | ✅通过 | 全站 `textDisabled` 仅用于装饰图标/禁用态（`MinusCircleOutlined`、滑块等） |
| 15 | 数字必须 dp-num | ✅通过 | 计数、编号(`dp-mono`)、百分比、字数、「2 个工作日」均等宽 |
| 16 | 375 下摘要不得竖排 | ✅通过 | 375 实测三段数字 `top` 全 = 3071（同行），标题 1 行，`overflow=0` |
| 17 | 轻量档不得做成残缺 BRD | ✅通过 | 轻量档为独立轻路径：4 字段 + 中性承诺条 + **无任何进度条**；smoke 断言"轻量档不出现资料完整度"通过 |
| 18 | 零网络请求 + 保留披露胶囊 | ✅通过 | 实测 `REMOTE_REQUESTS=0`（仅 5 个 `file://` 本地资源）；内联 agent 卡头有「脚本化原型」胶囊 |

**红线结果：17 通过 / 0 违反 / 1 有争议（第 13 条，见裁决）。**

---

## 3. 独立验证结果表

| 检查项 | 实测值 | 方式 |
|---|---|---|
| 重跑构建 | **exit 0**，3054 模块，`built in 6.48s` | 实测 |
| 重跑 smoke | **81 项断言，通过 81，失败 0**（自报 81/0 属实） | 实测 |
| 页面真渲染（前置哨兵） | `.dp-topbar` ✓ `.dp-shell` ✓ innerText=**1865** 字符 DOM 节点=**544** | 实测（file:// + headless Chrome） |
| console 错误 | **0 条** | 实测 |
| 远程网络请求 | **0 条**（`REMOTE_REQUESTS=0`） | 实测 |
| 横向溢出（375/768/1024/1280/1440/1920） | **全部 overflow=0** | 实测 |
| 顶栏高度六档 | 375→**141**，768/1024/1280/1440/1920→**61（恒等）** | 实测 |
| 顶栏导航项数 | **5**（无第 6 项） | 实测 |
| 首页区块垂直顺序 | 置顶决议(205)→健康度(635)→双栏Release(880)→**需求入口(1318)**→快捷入口(1664) ✓ 与规格一致 | 实测 |
| 4px 强调条唯一性 | `#/home` 4px=1、3px=2；其余路由 4px=0 | 实测 |
| 完成度真实性（递增） | 空 0% → 现象(标题+现状) 40% → +量级 47% → +系统 67% | 实测 |
| **D1 反例**（填「加个导出」） | D1 由"已具备"**降为"待补充"**，pct 67%→47%，agent 正确给出"挪到期望结果"建议 | 实测 |
| **D3「说不清」** | 状态="说到一半"（**0.5**，非 0 非满分），pct +20（满分的一半权重） | 实测 |
| D4 可验证/抽象/空 | "当…时"→已具备、偏抽象→"说到一半"、空→待补充(0) | 实测 |
| D5 空话标题 | 「优化一下」→ 待补充(0)；具体标题 → 已具备 | 实测 |
| 语义色越界（评分区） | 命中 **0** | 实测 |
| 进度条填充色 | 仅 `rgb(54,67,186)` 品牌蓝 | 实测 |
| 空表单提交 | 被拦，中性提示 `还差 5 项必填，补上就能提交`；5 字段同时转 error（允许） | 实测 |
| 只填标题（轻量档） | 被拦 `还差 3 项必填` | 实测 |
| 切档不丢数据 | 完整档→轻量档后标题仍在（`门店盘点库存对不上账…`） | 实测 |
| 「无硬性期限」 | 勾选后 `checked=true` 且日期选择器 `disabled=true` | 实测 |
| 旧文案回正 | `请到工作台「需求提交」提交` → **false（已无）** | 实测 |
| agent 同时建议数 | 恒 **1** 条 | 实测 |
| **≤900 单列区块顺序** | **异常**：承诺卡→BRD助手→**公开列表→表单**（表单被排到最后） | 实测（见 P0） |
| 未动文件核实 | git status 显示 `theme.js`/`router.js`/`TopBar.jsx` **均未修改**；mtimes 与基线一致 | 实测 |

---

## 4. 变异测试结果（本项目铁律：写完断言必须证明它会红）

对**构建产物**（非源码）注入，跑 smoke 作探测器，还原后核 sha256：

| 注入 | 位置 | smoke 结果 | 断言是否咬人 |
|---|---|---|---|
| **M1**：入口大卡强调条 `width:4`→`8` | `dist/assets/index.js` | **exit 1**，`FAIL 入口大卡 4px 强调条全页唯一 [实际 0 处]` | ✅ **会红** |
| **M3**：删除 `.dp-row:focus-visible` 规则 | `dist/assets/style.css` | **exit 1**，`FAIL 可点卡片/列表行有键盘焦点环` | ✅ **会红** |
| **M2**：`scroll-margin-top:76px`→`0px` | `dist/assets/style.css` | **exit 0，81/81 全绿** | ❌ **不咬**（断言只查 `/scroll-margin-top/` 存在与否，值改了照样过 → 弱断言） |
| **M4**：删除真实 chip 文案「说不清，帮我定位」 | `dist/assets/index.js` | **exit 0，81/81 全绿** | ❌ **空转**（见下） |

**M4 是本轮最重的发现**：我把「涉及系统」胶囊的真实 label 从 `说不清，帮我定位` 改成 `X不清…`（即界面上**真的没有这个选项了**），断言"涉及系统含「说不清，帮我定位」合法选项"**仍然通过**。原因：`AgentDrawer` 用 `getContainer={false}` 关闭后 DOM 仍驻留，其脚本化回复文本里含同一字符串，`has(doc, ...)` 对整页 `body.textContent` 判定 → **假阳性**。构建师只把 2 条断言限定到 `.dp-demand-form-col` 内（修掉了 `用途与期限` 那一条），**这一类"断言范围过宽"问题并未系统性修复**（`说不清`/`验收标准`/`现状` 等串在 bundle 里各有 3–6 处来源）。

**还原核验**：M1/M2/M3/M4 全部还原后，`style.css` sha256 = `e9448e6a…`、`index.js` = `49912ce4…`，与备份**逐位一致**（`match=true`）。干净跑 exit 0、变异跑 exit 1，护栏整体非空转，但 M2/M4 两条是例外。

---

## 5. P0 / P1 / P2 问题清单

### P0（阻断交付）

**P0-1 · ≤900px 单列下表单被排到公开列表之后（spec §3.4 违反）**
- 现象（实测，375/768/900 三档一致）：单列视觉顺序 = **受理承诺 → BRD 协作助手 → 最近提交（完整 6 条、高 712px）→ 需求登记表单**。表单在最底部。
- 根因：`Workspace.jsx:835` 右栏容器写了 **内联 `style={{ display:'flex' }}`**，内联样式优先级高于 `global.css` 的 `@media (max-width:900px){ .dp-demand-side{display:contents} }`，导致该条 CSS **完全失效**（实测 `sideDisp=flex`，非 `contents`）；此时 `.dp-demand-form-col{order:3}` 让表单作为 grid item 排到右栏（order 0）**之后**。global.css 里"用 `.dp-demand-side{display:contents}` 让右栏 flex 容器消失"的注释与真实行为不符。
- 影响：移动端用户必须**滑过整张公开列表**才能看到表单；C3「公开列表与表单同屏」在窄屏反被破坏（列表把表单挤出首屏）。
- **修复（二选一，见第 8 节处方）**。

**P0-2 · 一条核心断言被证明是空转的（违反交付说明 §6.2b）**
- 证据即 M4：删掉真实 chip 后断言仍绿。这类"测不到东西的断言跑完还是绿的"正是本项目历史上吃过亏的形态。
- 影响：smoke 对「说不清」这一关键能力**没有真实保护**；同类 body-wide 断言（`验收标准`、`现状`）均可能在 UI 缺失时假通过。
- **必须修复**：把涉及这些字符串的断言全部限定到各自面板作用域（或先移除常驻 Drawer 节点再断言整页）。

### P1（建议修复）

**P1-1 · `brdTemplates` / `demandTracks` 是死数据，"模板填写引导"非数据驱动**
- `mock.js` 导出了两档字段定义（`brdTemplates.brd/light`）与档位元数据（`demandTracks`，含 `fieldCount`/`committed`），但**全项目无任何 import**（grep 确认）。表单字段在 `Workspace.jsx` 里硬编码，`fieldCount`（4/8）与 `committed` 文案未使用。
- 影响：spec §8.3 明确要求 `brdTemplates` 驱动字段；现在改模板要改 JSX，且"4 项/8 项"数字与实际字段靠人工同步，易漂移。

**P1-2 · 空的验收标准显示"偏抽象"提示（误导）**
- `Workspace.jsx:372`：`hint: i.state==='full' ? fullHint : partialHint`。`none`（未填）态错误地复用 `partialHint`。
- 实测 D4 空时行文案 = 「验收标准 **待补充** 验收标准**偏抽象**，加一句「当……时」会更清楚」——对一个**空字段**说"偏抽象"与事实不符。

### P2（可选优化）

- **P2-1 · M2 断言偏弱**：`smoke.cjs` 第 685 行只 `test(/scroll-margin-top/)`，值被改成 0 仍通过。建议断言"值 ≥ 顶栏高（≥60px）"。
- **P2-2 · ScaleChips 鼠标点击也显示焦点环**：`onFocus/onBlur` 切内联 outline 导致鼠标点击即出现 2px 蓝环（键盘可达是硬需求，但可在 `onMouseDown` 时抑制，或用 `:focus-visible`）。
- **P2-3 · 同一 `.dp-demand-promise` 类用于两张语义不同的卡**：轻量档表单内提示条与右栏"受理承诺"卡共用同一类名，被媒体查询 `order` 规则同时命中；当前无害，但语义混淆、后续易踩。
- **P2-4 · 新增 CSS 未做死代码清理**：`global.css` 末尾预留的 `.dp-chip-radio:focus-visible` 规则以注释形式保留（"本规则不启用"），属未决文本。

---

## 6. 对构建师 3 处折扣 + 「测试假阳性修复」的裁决

**折扣 1（新增 2 组 CSS）→ 裁决：一条合规、一条越界且无效。**
- `.dp-demand-entry-body` 在 ≤900 降内边距：**合规执行**。spec §2.5/§11.6 明确要求"大卡左内边距在 ≤900 降为 20"，属规格直接指定项，非装饰。
- `.dp-demand-side{display:contents}` + 4 条 `order`：**越界，且未达成目标**。spec §3.4 只要求"单列下承诺卡提到表单上方"，并**推荐用 CSS order**——但没有授权新增一个 `display:contents` 重排机制；更严重的是它因内联 `display:flex` 而**失效**，反而制造了 P0-1。**在"结果错误"面前，"动机合规"不成立。**

**折扣 2（ScaleChips 内联 onFocus/onBlur）→ 裁决：可接受，但非最优。**
零新增 CSS 的取舍成立，键盘可达（无障碍硬需求）已满足。代价是鼠标点击也亮焦点环，属可感知的小瑕疵（P2）。§5.2 原文允许"内联 outline **或** 新增一条 `:focus-visible`"，构建师选了前者，**在 spec 允许范围内**。建议改 `onMouseDown` 抑制以消掉 P2。

**折扣 3（新建议到来时清掉旧建议）→ 裁决：方向正确，实现有缺口但不违反铁律。**
- "任何时刻气泡里不得同时出现 2 条以上待改建议"是**硬铁律**；构建师用"新建议替换旧建议"实现，实测恒为 1 条 → **铁律满足**。
- 但 spec **未规定"点 chips 提问时是否清掉待改建议"**——这是规格的空白。构建师的填空（点 chips 时 `map(x=>({...x,pending:false}))`）**把待改建议降级为历史**，气泡里会出现"建议"与"历史建议"并存于视觉（虽只有 1 条 pending）。**我的填法**：点 chips 属"用户主动提问"，应**保留**当前待改建议（它是与表单状态绑定的），只追加 Q&A；这样用户问完还能看到"你这里还差一句"。当前实现会**丢**掉待改建议，属体验倒退（P2，非 P0）。
- 更干净的实现：建议气泡与 Q&A 气泡分区（建议区恒定 1 条，对话区可滚动），从结构上杜绝"多建议同屏"。

**「测试假阳性修复」→ 裁决：方向对、覆盖不全。**
限定到 `.dp-demand-form-col` 确实修掉了 `用途与期限` 那一条；但**同类问题成片存在**（M4 已证 `说不清` 仍假通过）。**只修个案、不修模式 = 没修。** 建议：断言前统一移除常驻 Drawer 节点，或把所有内容断言限定到对应 Panel 作用域。

---

## 7. 修正处方（精确到文件与做法，一行一条）

1. **P0-1**：`portal/src/pages/Workspace.jsx:835` —— 删除 `.dp-demand-side` 上的内联 `style={{ display:'flex', flexDirection:'column', gap:16 }}`，改用既有类在 `global.css` 表达（保持 `@media ≤900` 的 `display:contents` 生效）；或在 `global.css` 的 ≤900 规则里直接写 `.dp-g-spec > .dp-demand-side{ display:contents !important }` 覆盖内联。
2. **P0-1 复核**：修完后用 CDP 在 375/768/900 三档断言视觉顺序 = 承诺卡 → **表单** → 公开列表（表单 `top` 必须小于列表 `top`）；当前实测顺序相反。
3. **P0-2**：`portal/tests/smoke.cjs` —— 在断言整页文案前，先执行 `doc.querySelectorAll('.ant-drawer, .ant-modal-root').forEach(n=>n.remove())`（或移除 `[class*=drawer]` 常驻节点），并把 `has(doc,'说不清，帮我定位')`、`has(doc,'验收标准')` 等改为在 `.dp-demand-form-col` / `.dp-demand-side` 作用域内判定。
4. **P0-2 自证**：把「说不清」chip 文案临时改掉（如改 `X不清`），smoke **必须变红**；不变红说明仍未修好。参照 M4 的做法。
5. **P1-1**：`portal/src/pages/Workspace.jsx` —— 让 BRD/轻量表单字段由 `brdTemplates[track]` 渲染（或至少从 `demandTracks` 读 `fieldCount`/`committed` 用于卡头 desc 与承诺条），否则删除这两个未使用的导出。
6. **P1-2**：`portal/src/pages/Workspace.jsx:372` —— 把 hint 映射改为三态：`state==='full'?fullHint : state==='partial'?partialHint : (i.emptyHint||'这一项还没写，受理人会需要它')`；并在 `brdDimensions[key]` 补 `emptyHint`（`mock.js`）。
7. **P2-1**：`portal/tests/smoke.cjs:685` —— 断言从"存在 `scroll-margin-top`"升级为"解析出的 px 值 ≥ 60"（顶栏高），使其能咬住值被改小。
8. **P2-2**：`portal/src/components/demand/ScaleChips.jsx` —— `ChipRadio` 增加 `onMouseDown` 设 `suppressFocus`，`onFocus` 时若该标记为真则不点亮 outline；或改回 `global.css` 加唯一允许的 `.dp-chip-radio:focus-visible` 一条规则（spec §5.2 明文许可）。
9. **P2-3**：把轻量档表单内的提示条类名从 `dp-demand-promise` 改为独立语义名（如 `dp-demand-light-note`），避免与右栏 "受理承诺" 卡共用选择器；需同步 `global.css` 的 order 规则作用对象。
10. **P2-4**：`portal/src/global.css:593-599` —— 删除"未启用"的 `.dp-chip-radio:focus-visible` 注释块，避免留下与实现不符的规格残留。
11. **建议（规格层）**：spec 应在 §5.2 与 §3.4 之间补一句"右栏重排**不得**依赖元素自身已设内联 display"——本次 P0 正是这条空白的产物；同时把"唯一允许新增 CSS"从"1 条"改为"1 条 + §2.5/§3.4 执行必需的 ≤900 档规则"，避免构建师反复在合规性上做主观裁量。

---

## 8. 我认为规格可以批评的地方（独立审查方的立场）

- **spec §9-13「唯一允许新增 CSS = 1 条」与现实冲突**：§2.5、§3.4 本身就要求 ≤900 档的两处执行项（大卡内边距、单列顺序），但 §13 只放行 1 条焦点环。规格内部不自洽，逼得构建师"先做再解释"，这正是折扣 1 越界的土壤。**责任不全在构建师。**
- **spec §7.3 未定义"点 chips 提问时待改建议的存亡"**，构建师只能在空白里自选，被算作"折扣"并不公平；这条应写进规格。
- **smoke 断言的"作用域污染"问题，规格从未要求"断言必须限定作用域"**——它是本项目 lesson 6.2b 的自然延伸，但没写进验收清单。建议把它列为交付门槛。

---

*证据文件（均在项目根目录，审查方临时产物已清理，`portal/src/` 与 `portal/dist/` 未被修改，dist sha256 与原产物逐位一致）：*
`_review-build.txt` · `_review-smoke.txt` · `_review-cdp.txt`…`_review-cdp8.txt` · `_mutation.txt` · `_sha-cmp.txt` · `_sha-backup.txt` · `_shot-375-demand.png` · `_review_git.txt` · `_review_mtimes.txt`
