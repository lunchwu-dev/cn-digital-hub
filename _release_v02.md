# v0.2 · 数据源设计 + 公网发布

在 v0.1 的可交互原型之上，这一个版本补齐了产品方案里反复标注的「**本期最大成本变量**」——数据源与集成边界，并让原型真正跑到了公网上。

---

## 新增：数据源设计（`outputs/05-datasource-design.md`）

对门户**全部内容域**做了一次完整的数据源盘点与运营模式裁定。

### 盘点口径（可机械复核）

| 项 | 数量 | 说明 |
|---|---|---|
| `mock.js` 导出实体 | **35** | `grep -n "^export const"` 实测，不是 34 |
| 归并后内容域 | **22** | 13 生产域（D01–D13）+ 9 派生/配置域（P01–P09） |
| 非 mock 内容域 | **3** | 硬编码在组件里，按 mock 盘点必然漏掉 |

三处非 mock 内容域：顶栏通知列表（`TopBar.jsx:34-38`）、首次访问引导（`App.jsx:25-42`）、页脚披露文案（`App.jsx:187-188`）。

> 实体数 35 与域数 22 是两套口径，不可混算：`designLinks` 横跨 D10/D10b，`demandHistory` + `demandStatusMap` 同归 P02。

### 运营模式裁定结论

| 运营模式 | 域 |
|---|---|
| **全自动**（系统直连，人工零介入） | 监控三域（KPI / 趋势图表 / 稳定性状态）、工具运行状态链、关键联系人（若企微有 API） |
| **半人工 · 填链接自动抓取** | 设计资产聚合入口（Figma / 组件库 / 图标）——**前提未验证** |
| **半人工 · 人写正文 + 系统补字段** | 项目动态、里程碑、Release 兜底、需求提交（MVP） |
| **全人工** | 公告与决议、最佳实践、设计规范、FAQ 正文、工具目录字段、引导与页脚文案 |
| **派生 / 聚合** | 待补知识队列、需求记录（二期）、Agent 回复（二期）、通知列表 |

### 可复用的判定框架

- **模式谱系**：M1 全自动 / M2 半自动·填链接抓取 / M3 半自动·人写正文+系统补字段 / M4 全人工 / M5 派生 / M-combo 复合（目录 + 状态分链）
- **三条核心判据**
  1. 最大的成本变量不是「有没有 API」，而是「**权威源是否唯一**」。已有权威源的域，门户一律只做**投影**，不做状态机。
  2. 「出错代价高」要拆成**怕延迟**（监控 → 必须自动）还是**怕写错**（公告 → 必须人工）。同为高代价，结论相反。
  3. 到达频率 ≠ 人工不可持续，要看**触发方式**：事件驱动 → 人工可承受；节律驱动 → 人工必崩。频率决定自动化下限，触发方式决定人工上限。
- **Q3b 决策补强**：需要人判断时，再问「判断所需的正文是否已在某个系统里写好」——是 → M2，否 → M3。没有这一步，决策树会给出「M2 **或** M3」，而「或」不是结论。
- **两个必备前提**
  - M2 只适用于**匿名可访问**的源页面（Figma / 语雀 / Confluence / Jira 全需登录，抓不到）
  - 所有 M1 探活状态的枚举**必须含 `unknown`**（探活失败 ≠ 服务挂了，否则门户显示上一个状态等于对用户说谎）

### 关键实现契约

- **三个复合域必须拆链**：工具目录（目录人工 + 状态探活自动）、FAQ（正文人工 + 计数自动）、Release（主路径直连 + 小系统兜底手填）。只做一半的后果是状态永远是手填的**假绿**。
- **P01 反馈闭环在当前实现中不存在**：`Knowledge.jsx:196-211` 的 `markHelpful` 只写组件内 `useState` + 弹 toast；`faqs.helpful/notHelpful` 是静态值、点多少次都不变；无落库、刷新即失。落地必须新建 `faq_feedback` 事件链路（字段 `faqId` / `helpful` / `ts` / `dept`），且闭环五步（写入 → 聚合 → 待补队列 → 认领 → 关闭）各自要有 Owner。
- **监控拉的是「聚合序列」不是「单标量」**：`opsCharts.series` 是 3 条 7 点序列、`opsKpis.spark` 是 7 日序列。按「读 1 个标量接口」估工作量，实现时会翻倍。
- **无登录架构的两条合规红线**：身份类数据源只用于展示、不用于追踪个人行为；`orgPeople.email` 禁止用于埋点关联。

### 工作量与集成预算

乐观 **6–8 周** ｜ 悲观 **12–18 周**（硬集成，含联调、权限、口径对齐、异常兜底）。
**一期 MVP 集成预算上限建议 ≤ 8 周**（1 名后端工程师全职）。

超出时的砍域顺序：项目进度接入 → 设计资产聚合 → 人员自动化 → **最后才动 Release 与监控**。
**不可砍项是反馈链路**（1.5–3 周）：成本低，但它是产品方案定的首要运营指标来源，也是二期 Agent 的活水。

### 需甲方确认的 9 个问题（Top 3 决定自动化分支）

1. **Q1** 发布系统是什么？是否提供按系统聚合的版本/状态 API？
2. **Q2** 监控：Grafana 还是自建 Prometheus？是否开放只读 API？
3. **Q3** 项目进度权威源是 Jira、Meego 还是两者并存？**双源并存会直接推翻「单一权威源」假设。**

---

## 新增：公网发布支持

原型已发布为公网可访问站点，验证渲染无误。

- 新增 `portal/server.mjs`：零依赖单端口静态服务（读 `PORT`、绑 `0.0.0.0`），用于本地 HTTP 预览
- `portal/package.json` 增加 `start` 脚本
- `portal/README.md` 补充本地预览与公网发布的正确路径说明

### 踩坑记录（重要）

平台发布沙箱中，**`node` 型 http-service 会全部返回 404**——接口返回「发布成功」、`verified: true`，但所有路径实际都访问不到（网关 `Eo-Cache-Status: MISS`，无可用后端）。

排查判据：本地 `node server.mjs` 与 curl 均正常 → DNS 正常解析到真实 LB → 换新应用/新沙箱/新域名仍 404 → **纯静态探针站一发即 200**。

**结论：项目是纯静态产物时，不要包 Node 进程。** 把 `dist/` 复制成独立目录，以 `language:"static"` + `entryHtml:"index.html"` 发布。判据是返回结果里的 `deployedAs` 字段——只有 `"web-page"` 才代表走平台内置静态服务（`"http-service"` 不可靠）。

---

## 过程留痕（可复现的质量链）

| 环节 | 产出 | 结论 |
|---|---|---|
| 需求发现 | `outputs/_research/datasource-phase1-discovery.md` | 35 实体 → 22 域 + 3 非 mock 域 |
| 第 1 轮独立审查 | `outputs/_verify/datasource-phase1-review.md` | **REVISE**：3 致命 + 6 重要 + 4 建议 |
| R1 修订 | 同上（含修订记录表） | 全部落地 |
| 第 2 轮定向复验 | `outputs/_verify/datasource-phase2-review.md` | **PASS**：13/13 已修复，0 阻断项 |
| 主理人独立核验 | — | 亲自 grep 确认 35 实体与行号逐一对上 |

第 1 轮审查抓到的三个真问题，值得记下来——它们是「看起来完整」的方案的典型漏洞：

1. **盘点自称「无遗漏」，但实际漏了 3 处内容域**。方案按 `mock.js` 清点，而通知/引导/页脚文案硬编码在组件里。**「我们盘点了全部」这句话在验证之前不成立。**
2. **实体数是 35 不是 34，且原先「28 + 6」两个数都对不上**。清点靠印象而非机械方法（grep），遗漏几乎是必然的。
3. **一个被标为「确定」的派生域，其依赖的源头在代码里根本不存在**。待补知识队列被设计得很好，但反馈本身只存在内存里。**设计的完整性掩盖了实现的空壳。**

---

## 变更文件

```
新增  outputs/05-datasource-design.md                       数据源设计主文档
新增  outputs/_research/datasource-phase1-discovery.md      需求发现（含 35 行实体对照表）
新增  outputs/_verify/datasource-phase1-review.md           第 1 轮审查（REVISE）
新增  outputs/_verify/datasource-phase2-review.md           第 2 轮复验（PASS）
新增  portal/server.mjs                                     本地预览用零依赖静态服务
修改  portal/package.json                                   增加 start 脚本
修改  portal/README.md                                      补充部署路径说明
修改  .gitignore                                            排除发布副本与根目录临时文件
```

---

## 已知取舍

- **保持原型可独立运行**：`portal/dist/` 仍是可双击打开的离线产物，本次未改动原型 UI 与交互
- **数据源设计定的是「边界」而非「实现」**：9 个待确认问题中的 Q1–Q3 需要甲方给出答案后，才能把 M1/M3 分支落到具体接口契约
- **`.gitignore` 排除 `portal-site/`**：它是 `portal/dist/` 的逐字节副本（已核对 sha256 一致），仅用于发布，不重复纳入版本管理
