# v0.3 · 站点结构重构 + 业务需求页改造 + 员工标签体系与个人主页

在 v0.2 的数据源设计与公网发布之后，这一个版本把产品方案里**最核心的两块交互骨架**落到了可运行原型上：
**面向组织的信息发现**（谁懂什么、组织速查）与**面向协作的需求流转**（业务需求怎么提、提了到哪一步）。

---

## 零、本版是怎么合出来的（合并交付说明）

这两块骨架是**两条并行的任务线**：A 线做站点结构重构 + 业务需求页改造，B 线做员工标签体系与个人主页。
两条线在**同一条工作区**上交错推进（事后核对过程留痕，两者有 7 次提交交错），因此本版**不做拆分，合并为一个 v0.3 提交**。

**为什么不能拆**（这是技术判断，不是图省事）：

两条线在源码层共享同一批**装配文件**——`router.js` / `App.jsx` / `ui.jsx` / `mock.js` / `global.css`。
其中存在**原子耦合**：导航重构要在 `router.js` 里加 `org`、`demand` 两个归属，而员工主页的 `people`、`tags`
归属改动**落在同一个代码块内**（`router.js:13-14` 与 `NAV_OF:25/34/35/36` 是一处改动的两面）。
要让其中一侧单独构建，就必须连带引入另一侧的页面组件，最终会把整条另一条线都拖进来——
**拆分后的中间态无法独立构建**。

而「每个提交都应当能构建」是本项目的硬约定（`portal/dist/` 是可双击运行的交付产物，纳入版本管理）。
因此合并是**唯一能保持交付物完整的选择**。

**合并后的验证口径**：不再分别验收两条线，而是把两个大任务的全部需求**合并成一份需求清单**，
逐条判定「满足 / 部分满足 / 未满足」，再跑完整回归（详见 §五）。结论是 **11 条需求：满足 9 / 部分满足 2 / 未满足 0**，
两条「部分满足」都有明确的语义偏移或数据缺口说明，不含糊过去。

**合并的代价**（如实说明）：

- 无法用「回滚单个 tag」把站点重构与标签体系分别回退，本版是一个原子交付
- 两条线的**产物是同一份**（`dist` 是合并 bundle），不存在「两个版本各自一份产物」
- 源码层面仍可通过文件归属看清各线范围（见文末「变更文件」按线标注）

---

## 一、站点结构重构

### 1.1 工作台退回「工具目录」本职

重构前，工作台里塞了三个 Tab：工具导航、组织速查、需求提交。它因此既不像工具目录，也不像业务入口。

**本次裁决**：工作台**只保留工具导航职能**（`#/workspace`，左侧分组目录 + 右侧工具卡网格）。
组织速查与需求提交从工作台**整体迁出**，升为一级栏目。对应地，`Workspace.jsx` 里那套需求提交流程
（`DemandView`，621 行，含一份与需求页重复的评分引擎）**整段删除**——避免同一套逻辑存在两份实现。

### 1.2 两个新一级栏目：组织速查、业务需求

顶部导航从 5 项扩到 **7 项**，新增两项插在「工作台」之前：

```
首页 · 今日 Hub → 信息中心 → 监控运营 → 知识中心 → 组织速查 → 业务需求 → 工作台
```

排序理由是**信息发现类排前、工具目录类收尾**。

**`NAV_OF` 映射的取舍**：`people`（员工个人主页）与 `tags`（标签反查）的路由前缀**保持在 `#/workspace/...` 不变**，
但导航高亮归属**改到「组织速查」**。这样做的原因是：路由搬家会切断所有既有深链与搜索结果，
而高亮归属才是用户感知「我在哪个栏目」的来源。**路径归属与导航归属解耦**是本轮的一条可复用做法。

**导航分档阈值重算**：7 项导航挤占顶栏空间，原阈值（1272 / 1012）作废，重算为
**T1 ≥1188 / T2 940–1187 / T3 768–939 / T4 ≤767**。核算依据是 7 项 × padding `0 11px` ≈ 907px，
减去内层 gap 后仍余 **285px**，放得下。

### 1.3 Agent for Digital：从顶栏按钮改为常驻悬浮 doodle

原先是顶栏右侧一个按钮（`.dp-agent-btn`）。问题在于：它被当成「一个功能入口」，
而它实际是**跨站能力**——用户在任何页面都可能想问一句。

**本次裁决**：改为站点右下角**常驻悬浮 doodle**。
- 形态：白底 + 1.5px 品牌蓝描边 + 品牌蓝图标，52px 正圆，轻投影（**不做品牌蓝实心**，避免与主操作按钮抢层级）
- 位置：`position:fixed; right:24px; bottom:24px`，**永远不做「躲进页脚」**——它是常驻能力，不是装饰
- 层级：`z-index:900`
- 窄屏（≤767）：缩小到 48px、`right/bottom:16px`，**仍常驻**，配合内容区 `padding-bottom:72px` 避免遮挡
- 提示态：8px 品牌蓝圆点（首次使用后一次性），**无数字气泡、无红黄绿**
- 无障碍：`aria-label`、`:focus-visible` 2px 黄环、Esc 关闭、`prefers-reduced-motion` 下关闭过渡

点击后打开的是**右下角轻面板（380×560）**，不是原先的全高 Drawer——因为它是「就地问答」，不该遮挡整页。

### 1.4 站点反馈并入 Agent

原先顶栏有一个独立的反馈按钮 + Modal（四选项下拉 + 300 字文本域）。

**本次裁决**：不再保留独立入口，**把反馈做成 Agent 的结构化能力**。原来那四个选项
（信息架构 / 内容缺失 / 显示问题 / 新功能建议）**保留为 `kind` 参数**，由 Agent 在对话中内联询问，
而不是先弹一个表单——「先说明来意，再归类」比「先选分类，再描述」更符合人的表达顺序。

### 1.5 顺带修正的历史文案错误

导航重构让若干条**指向旧路径的文案**全部失效（它们还在说「首页『提交需求』大卡或工作台『需求提交』页」）。
本轮一并改写为指向 `#/demand/new`：Agent 的 sso / 权限两条回复、知识中心文章正文、以及 mock 里的路径描述。

---

## 二、业务需求页改造

### 2.1 一级页：状态楼层 + 需求列表 + 显著新增入口

需求页拆成**一级页（看）**与**二级页（提）**两页。一级页 `#/demand` 由三块组成：

**① 页顶「状态楼层」** —— 5 格大数字横向一行：

| 格 | 内容 |
|---|---|
| 待处理 / 进行中 / 已完成 | 各状态需求数量 |
| 异常告警 | 待跟进事项数与责任人 |
| 新增需求 | 显著入口（另在列表上方再放一次整行入口） |

**「异常告警」的定义是四条可计算规则**，不是手工标记：SLA 超期 / 承诺临近 / 阻塞标记 / 长期滞留。
责任人以中性灰摘要呈现（`责任人 顾一鸣`、`责任人 顾一鸣 + 待指派`、`责任人 3 人 · 含待指派`），
按人数与是否含「待分配」分三种表述。

**② 公开列表** —— 不折叠（≥6 条直接展示）。刻意**不用 Collapse**：需求列表是这一页的主体内容，
折叠它等于把主内容藏起来。列表行也**不显示成熟度/贡献度排名**——这一页回答的是「事办到哪了」，不是「谁更行」。

**③ 语义色边界（本页铁律）** —— **计数永不使用语义色**。异常告警用的是
**品牌蓝数字 + 品牌蓝图标 + 中性胶囊文案**，红/黄/绿**彻底不出现在本页**。
这条规则的作用域是**楼层**；列表内单条记录的状态胶囊仍可用语义色（那是「单条标记」，不是「汇总统计」）。

### 2.2 二级页：表单 + 嵌入到表单控件里的 Agent

新增需求入口点击后进入二级页 `#/demand/new`，交互形态是「**表单 + 嵌入表单控件的 agent**」。

**Agent 的嵌入选的是「2 级半」，不是两侧极端**：
- 否决**纯右栏**（agent 只在一个角落自言自语，与表单无关）
- 否决**替换控件**（让 agent 接管输入，反而剥夺用户的控制感）
- 采用：**字段级图标就地给建议** + **右栏独立协作卡**

**双档模板分流**（延续 v0.1 的设计，本轮保留并搬入二级页）：
- **轻量档**（权限/其他，4 项必填，**不评分**）
- **完整档**（功能/系统，BRD 8 字段，**实时完整度**）

一刀切 BRD 会赶走只想申请个权限的内部工程师，纯表单又让业务方说不清需求，所以两档并存。
完整度进度条**只用品牌蓝、永不随分数变色**，且**不出现「得分/扣分/满分」字样**——它是「还差哪项」的提示，不是考试。

**两个 Agent 入口职责二分**：悬浮 doodle = 跨站问答/反馈登记（无表单上下文）；
二级页内嵌 = 当前表单的写作助手。**二级页内不挂 doodle**，避免两个 agent 抢注意力。

**表单草稿保留**：返回一级页后再进来，**草稿 keep-alive 保留**；只有提交成功后才清空。

---

## 三、员工标签体系与个人主页

### 3.1 双轴正交标签模型

标签体系的设计要点是**两个轴互相正交**，一个标签只能落在一个轴上：

| 轴 | 含义 | 分组 |
|---|---|---|
| **领域轴** | 「懂哪个业务/技术领域」 | AI 与智能、业务系统域、数据域、平台与基础设施、组织与流程域 |
| **能力轴** | 「以什么方式贡献」 | 工程技术、产品与设计、数据与分析、项目管理与协作 |

**为什么必须正交**：如果把「懂供应链」和「会写代码」放进同一个扁平列表，
反查时就无法回答「谁既懂供应链又会写代码」——而这是团队组队时最常问的问题。

### 3.2 员工个人主页

主页展示：页头身份 + **双轴标签矩阵**（分轴呈现，紧凑态 + 放大态两档）+ 右栏贡献说明。

**每个标签有两条并列的度量**，这是这套设计的核心：

| 度量 | 表达 | 来源 |
|---|---|---|
| **兴趣 / 自评** | 4 级圆点阵 | 本人自评 |
| **实证** | 4 段填充轨 | 系统自动核算（来自最佳实践、文章、项目动态、公告、Release 的**署名**） |

**关键设计取舍：当自评高而实证为空时，系统不隐藏、不惩罚，而是把两条并置展示。**
用户在页面上会看到「●●●○ 主动投入」与「░░░░ 仅自评 · 暂无公开贡献」同时出现——
这个对照本身就是信息：*这位同事很投入，但产出还没有沉淀成可被检索的资产*。

这不是揭短，而是**召回入口**：把空处显示出来，才有机会被补上。

### 3.3 标签反查页

`#/workspace/tags`：按轴切换（全部 / 领域轴 / 能力轴）+ 词选择 chip 组（按域分组分行）+ 结果区。
结果**按姓名排序并在页面上主动声明「不按任何贡献度排名」**——这是产品红线，需要用户看得见。
已归档标签（deprecated / merged）单独折叠区，标出「已停用」或「新名 ← 原『旧名』」，不静默丢弃。

### 3.4 空态设计（本轮重点修正的方向）

反查「谁懂 RAG」这类问题，会得到 0 个人。**这是数据事实，不是缺陷**——真实的部门里
AI 领域本来就可能只有一两个人。

一开始的修法建议是「补几个人的 AI 标签」，**这个建议被否决了**：那等于往演示数据里注水，
用数据掩盖设计问题，而且会让 mock 丧失「如实反映人员构成」的使命。

**实际采用的做法是「把空态设计好」**：
- 空的是**整个域** → 告诉用户这是个组织诊断信号：
  「「AI 与智能」这个领域目前还没有人登记。词表已预留该领域的 6 个标签，等待第一位贡献者——这类空档本身也是部门能力盘点的一个信号。」
- 空的是**多选交集** → 给出下一步：
  「这些标签的交叉暂时没有落在同一个人身上。可以去掉一两个条件，或分别查看每个标签下的人。」

**空态文案的两条纪律**：① 必须诚实（承认没有，不粉饰）；② 必须给出**下一步**或**诊断信息**，
否则只是把一句话换成另一句同样没信息量的话。

### 3.5 空态文案与视觉噪音的收敛

- `暂无实证` → **`暂无公开贡献`**（更准确：不是「没有实证」，是「没有**公开**贡献被系统统计到」）
- 吹牛态提示行 → **`这项仅有自评，系统暂未找到对应的公开产出。`**
  —— 统一用**第三人称**（个人主页是只读的，不放「编辑我的资料」入口，也不暗示「这是你的页面」）
- 全 0 的贡献密度曲线 → **不再画一条贴底的平线**（那读起来像坏图），改为一行「近 6 月暂无公开贡献」

**视觉噪音按自评等级分两档收轨**，这是本轮最需要小心的一处改动：

```
自评 ≤ 2 级且无实证  →  收轨：不画空槽，只留一行极淡的「暂无公开贡献」（噪音主体）
自评 ≥ 3 级且无实证  →  保留完整双行：点阵 + 虚线空槽 + 文案 + 提示行（上面说的「吹牛态」）
```

**为什么不能一刀切全收**：吹牛态依赖「自评行 + 实证空槽」**并置**才成立。
把所有空槽都收掉，就只剩下「主动投入」，那个诚实的并列就被消解了。**收噪音不能收掉信息。**

---

## 四、本次修复的缺陷

两位质量审查官对两条线各做了一次 5 维评审，判定均**不可直接交付**（v0.3 线 20/25，标签线 18/25）。
以下缺陷在本次一并修复。

### 4.1 P0：需求页「期望完成时间」一填就白屏

**现象**：在新增需求二级页填了「期望完成时间」并提交，回一级页列表**整页白屏**。

**根因**：`DatePicker` 的 `onChange` 存进草稿的是 **dayjs 对象**，
而列表渲染处直接把它当成 React 子节点输出 → `Objects are not valid as a React child`（React error #31）。

**修复位置很关键，第一版修错了地方**：起初想改 `onChange` 让它存字符串，实测**没修好还换了个地方崩**——
因为 `DatePicker` 的 `value` 需要 dayjs 对象来回填，喂字符串会让 antd 调用 `.year()` 抛
`TypeError: t.year is not a function`，**表单子树崩、连提交按钮都点不到**，等于把白屏从列表页挪到了表单页。

**正解是「存的时候保持 dayjs、提交组行数据时格式化」**——`onChange` 不动，在写进行数据前转成字符串。

**已加回归门**：一条端到端断言（填日期 → 提交 → 回列表 → 该行可见且零 React 错误）。
并做了**变异自证**：把修复改回原样，该断言**恰好一条翻红**，其余全绿；还原后全绿。

**变异自证在提交前一度失效，已修复**（这是一处必须如实记录的过程）：站点重构把需求表单从
`Workspace.jsx` 迁到了 `DemandNew.jsx`，但 `mutation.cjs` 里的 M1（守「右栏容器不得写内联 display」，
即本缺陷的复发守卫）**仍指向老文件**，find 串在新位置 0 命中；M4 同样失效，且承载修复的
`DemandNew.jsx` **根本没进 `TARGETS` 洁净锚点**。后果是这条守卫会走「未命中 → 跳过」而**永远不会红**。
修复方式是把 M1/M4 改指 `DemandNew.jsx`（:682 / :123，实测各 1 命中）、把该文件纳入 `TARGETS`，
并重跑全量：**6/6 变异均真变红，每条恰好 1 条断言红，收尾 sha 全部归位、工作树洁净**。

> 教训与上一轮同源：**名义覆盖 ≠ 实际覆盖**。上一轮是「断言读源码文本冒充读渲染结果」，
> 这一轮是「变异指向的文件里已经不存在那段代码」。两者都会显示为绿色。

### 4.2 P1：首页与需求页数字对不上

首页入口大卡的计数读的是静态示例数据，需求页楼层读的是实际需求记录，
**两处数据源导致同一批数字互相矛盾**。已统一为同一份数据源。

### 4.3 P1：入口卡次要按钮跳错页

首页入口大卡的次要按钮写着「先看提交指引」，点击却落在需求**列表**页。
站内确实没有「提交指引」这个承载页，所以**不新建页面、也不留一个跳错地方的按钮**，
而是把文案改成与落点一致的「先看看别人提了什么」。

### 4.4 P2 系列

| 项 | 问题 | 处置 |
|---|---|---|
| 死代码 | `Workspace.jsx` 内 621 行需求提交流程（含与需求页重复的评分引擎） | 整段删除 + 清理连带 unused imports |
| 测试盲区 | 楼层「多责任人」分支从未被覆盖（含 2 条不可达分支） | 抽出纯函数，6 组输入覆盖全路径 |
| 测试稳定性 | `todayStamp` 取真实系统时间，测试与截图随日期漂移 | 改为可注入，缺省仍取真实时间 |
| 证明强度 | 窄屏断点顺序只在 CSS 规则层面断言过（在样式表文本里 match 到 `order:1/2/3` 三个字符串，**不是实测**） | 该文本门**改名标注为「非实测」**；新增**真实几何门**：headless Chrome + CDP 读 `getBoundingClientRect().top`，900 档验单列顺序、1280 档验双栏；并新路由纳入几何扫描 |

---

## 五、测试与验证

### 5.1 回归结果（本机实跑，非自述）

| 项 | 结果 |
|---|---|
| 构建 | `vite build` exit 0（3061 modules） |
| 构建可复现 | 重建后产物 sha256 **不变**：`index.js` = `C97F577ADA9E497D…`（1,364,909 B） |
| 冒烟断言 | **152 项，通过 152，失败 0，exit 0**（jsdom，产品 bundle 内联 CSS 真渲染） |
| 真实几何扫描 | `shots.cjs`：11 档宽 × 10 条路由 = **110 个格里点**，含 `#/org`、`#/demand`、`#/demand/new` |
| 语义色铁律 | 13 条路由真实浏览器逐条扫（computed style）→ `#/ops` 之外真越界 **0** |
| 窄屏顺序 | 900 / 1280 / 375 三档**真实几何实测**通过（`getBoundingClientRect`，见 §5.3） |
| 离线交付 | `dist/index.html` 双击（`file://`）可开；无 `type="module"`、无 `crossorigin`、零网络请求 |
| 变异自证 | `tests/mutation.cjs`：**6 条变异 6 条真变红**，每条恰好 1 条断言红；收尾 sha256 全部归位、工作树洁净 |
| 断言成色 | 逐条分类：实测门（读 `getComputedStyle`/`getBoundingClientRect`/DOM）约 60 条；已标注的文本门 1 条（≤900px 规则级）；纯函数探针 5 组。残留 4 条未标注文本门（见 §5.4） |

### 5.2 需求覆盖验收（合并口径）

把两个大任务的需求合成一份清单后逐条判定，附可核验证据，见 `outputs/_acceptance-merged.md`。
结论：**满足 9 条 / 部分满足 2 条 / 未满足 0 条**（A 组 6/1/0，B 组 3/1/0）。两条「部分满足」为：

- **A5 语义偏移**：「需求提交页展示列表」——列表落在一级列表页（`#/demand`）、表单落在二级页（`#/demand/new`），
  是信息架构上的**有意分工**，与字面表述存在偏移。按合并后的 IA 判定为满足，但**记录此偏移供决策**。
- **B4 空态覆盖**：「AI 与智能」域当前 0 人——空态文案已设计到位，但数据层为空
  （这是**有意不注水**的结果，见 §3.4）。且空态断言目前多是纯函数探针，未走点击→重渲染的完整 E2E。

### 5.3 验证方法上的三条做法（本轮沉淀）

1. **变异测试**：断言写完，要故意把被测代码改坏，确认断言**真的会红**。
   本轮对两处做了这项验证，两处都精确翻红且只翻相关的几条：
   - P0 修复改回原样 → **恰好 1 条**翻红（`React error #31`），其余全绿
   - 吹牛态阈值改乱 → **恰好 3 条**翻红，失败信息直接指出「分档判据可能把吹牛也收轨了」

   并在合并收尾时把变异套件扩到 **6 条**，逐条实跑：**6/6 均真变红，每条恰好 1 条断言红**。

   **一条永远不红的断言，比没有断言更危险。** 更危险的是**看起来在红、实际永远不会红**——
   本轮就抓到两次（见 §4.1 与下条），每次都是「覆盖的名义 vs 实际」不一致。
2. **区分「实测门」与「文本门」**：同一个测试文件里，两种成色的断言看起来一样是绿的。
   读**渲染结果**的（如 `getComputedStyle` 实测楼层零语义色）才是真证据；
   读**源码文本**的（如在样式表里 match 到 `order:1/2/3` 三个字符串）只是规则级检查。
   本轮把后者**明确改名标注为「非实测」**，并新增真实几何门来承担顺序验证。
   jsdom 无布局引擎（`getBoundingClientRect` 恒为 0），凡是位置/顺序/尺寸的断言，
   必须在真实浏览器里量——本项目的通道是 headless Chrome + CDP。
3. **产物哈希判据**：涉及「产物是否反映当前源码」的判断，一律用**磁盘字节数 + SHA256**，
   不用构建工具打印的可读体积（那个会四舍五入）。并核 `dist` 的 mtime **不早于** `src/` 最新 mtime。

### 5.4 独立复验的最终裁定

本版在提交前做了一次**独立复验**（审查官以全新视角、不采信任何既有结论），结论经**一轮驳回后上调**：

| 轮次 | 裁定 | 理由 |
|---|---|---|
| 第 1 轮 | **不支持采信** | 变异套件 M1/M4 因代码迁址而 0 命中 → 关键守卫的自证已空转（见 §4.1） |
| 修复后 | **支持采信** | 6/6 变异各 1 次命中、sha 6/6 归位、因果链独立复现 |

复验的关键动作不是「读日志确认红了」，而是**反向检验因果链**：
用项目自带 React 实跑，确认断言读的是 `.dp-demand-side` 的**内联 style 属性**（`/display\s*:/` 判定），
再确认注入 `style={{display:'flex',...}}` 后 React 真会把它序列化进 DOM 属性、从而让断言翻红；
pristine 态则保持绿。**「注入 → 属性出现 → 断言翻红」这条链被独立跑通**，空转风险才算闭环消除。

### 5.5 已知残留（本版未修，如实记录）

- **4 条未标注的文本门**：`scroll-margin-top`、`:focus-visible`、`display:flex`、居中栅格，
  均是「规则存在性检查」而非渲染实测。其中「居中」已由 `shots.cjs` 真实几何覆盖；
  其余三条不涉及顺序/位置，风险低，但严格说不构成实测证据。
- **数据层断言无第三方交叉验证**：楼层计数、`PEOPLE_BY_TAG` 覆盖数等由实现侧的断言与复算得出，
  无独立的第二数据源可对账。
- **`shots.cjs` 需真实 Chrome**：不联网、无 Chrome 的机器上跑不了，该证据链在 CI 外无法自动重放。

---

## 六、已知取舍

- **原型仍是脚本化原型**：Agent 的回复是关键词匹配，不是真实模型；数据为示意数据，页脚已声明
- **反馈链路尚未闭环**：Agent 能结构化收集反馈，但落库与流转（写入 → 聚合 → 待补队列 → 认领 → 关闭）
  在当前实现中不存在，需要后端配合（沿用 v0.2 数据源设计的结论）
- **`min.zhou` 作为「吹牛态」样本保留**：他挂满 8 个标签而公开贡献为 0，
  是有意保留的**极端样本**，用来验证「诚实并列」在数据难看时依然成立。全库这类样本只有 2 人
- **能力轴当前拿不到实证**：证据映射只覆盖领域轴，所以能力轴标签的实证恒为空。
  这是已知的结构性边界，需要在数据源侧补映射规则

---

## 变更文件

> 标注：**[A]** = 站点结构重构 + 业务需求页改造线；**[B]** = 员工标签体系与个人主页线；**[+]** = 两条线共用的装配文件（合并后无法按线切分，见 §零）。

```
── A 线：站点结构重构 + 业务需求页改造 ──────────────────────────
新增  portal/src/pages/Org.jsx                        [A] 组织速查（一级栏目）
新增  portal/src/pages/DemandList.jsx                 [A] 业务需求一级页（楼层 + 列表）
新增  portal/src/pages/DemandNew.jsx                  [A] 业务需求二级页（表单 + 内嵌 agent / P0 修复点）
新增  portal/src/components/AgentPanel.jsx            [A] Agent 轻面板（替代 Drawer）
新增  portal/src/components/FloatAgent.jsx            [A] 悬浮 doodle
新增  portal/src/components/demand/FieldAgent.jsx     [A] 字段级 agent 建议（内嵌表单控件）
新增  portal/src/components/demand/ScaleChips.jsx     [A] 量级胶囊（单选，含焦点环抑制）
新增  portal/src/components/demand/BrdAssistantCard.jsx [A] BRD 协作助手卡
新增  portal/src/components/demand/CompletenessBar.jsx  [A] 完整度进度条（纯品牌蓝，不变色）
新增  portal/src/components/demand/DemandEntryCard.jsx  [A] 需求入口卡
新增  portal/src/components/demand/DemandRecentList.jsx [A] 近期需求列表（供入口卡区）
删除  portal/src/components/AgentDrawer.jsx           [A] 旧全高 Drawer（被轻面板替代）

── B 线：员工标签体系与个人主页 ────────────────────────────────
新增  portal/src/pages/PersonProfile.jsx              [B] 员工个人主页
新增  portal/src/pages/TagBrowse.jsx                  [B] 标签反查页

── 共用装配文件（两线均改，构成原子耦合）──────────────────────
修改  portal/src/router.js                            [+] 导航 5→7、NAV_OF 路径/高亮解耦
修改  portal/src/App.jsx                              [+] 路由装配、Home 数据源、FloatAgent 挂载
修改  portal/src/components/TopBar.jsx                [+] agent 按钮移出、反馈入口移除
修改  portal/src/components/ui.jsx                    [+] 标签矩阵、空态文案、收轨分档
修改  portal/src/global.css                           [+] 楼层、悬浮件、响应式断点 v2
修改  portal/src/data/mock.js                         [+] 需求数据、标签词表、文案修正
修改  portal/src/pages/Workspace.jsx                  [+] 退回纯工具目录（删 621 行死代码）
修改  portal/src/components/GlobalSearch.jsx          [+] 标签维度检索
修改  portal/src/pages/Home.jsx                       [+] 入口大卡、统一数据源
修改  portal/src/pages/ArticleDetail.jsx              [+] 历史文案（旧路径指向修正）
修改  portal/src/pages/Knowledge.jsx                  [+] 历史文案（旧路径指向修正）
修改  portal/src/theme.js                             [+] 设计令牌扩展

── 测试基建（本版重点补强）──────────────────────────────────
修改  portal/tests/smoke.cjs                          断言 90 → 152（含 P0 端到端门、空态探针）
修改  portal/tests/shots.cjs                          路由 7 → 10、档位扫描 88 → 110 格、新增断言1c 真实几何门
新增  portal/tests/mutation.cjs                       变异自证套件（6 条变异 / 6 文件 sha 基线 / 自动还原）
修改  portal/package.json                             版本 0.1.0 → 0.3.0；补 test:shots / test:mutation 脚本

── 产物与文档 ──────────────────────────────────────────
修改  portal/dist/*                                   重新构建（index.js = C97F577ADA9E497D，重建后逐字节一致）
修改  portal/shots/*                                  28 张截图（含 11 张新页面）；旧图字节变化源于 PNG 时间戳元数据
新增  _release_v03.md                                 本发布说明
新增  outputs/delivery-*/                             三个交付包（站点重构 / 需求入口增量 / 个人主页）
```

> 计数：新增源码 13 · 修改源码 12 · 删除源码 1 · 新增测试 1 · 修改测试 2。合计提交 118 个文件。

---

## 过程留痕

| 环节 | 产出 |
|---|---|
| 站点重构施工规范（含逐条裁决） | `outputs/02b-site-restructure-spec.md` |
| 需求入口增量设计规范 | `outputs/02a-demand-entry-design-spec.md` |
| 两条线质量审查 | `outputs/_audit-v03.md`、`outputs/_audit-v04.md` |
| 空态改造方案与文案清单 | `outputs/_audit-v04-emptystate.md` |
| v0.3 返修报告 | `outputs/_fix-report-v03.md` |
| 合并施工报告 | `outputs/_fix-report-merged.md` |
| 测试基建补强报告 | `outputs/_fix-report-tests.md` |
| 需求覆盖验收矩阵（合并口径） | `outputs/_acceptance-merged.md` |
| **合并交付独立复验报告** | `outputs/_verify-merged-v03.md` |
| 变异套件修复后全量实跑日志 | `outputs/_mutation-run.txt` |

> 上述「验收矩阵」与「独立复验报告」是本版的两条核心证据链。
> 前者把所有数字做了**独立复算**（不采信任何先前的审计结论），并单列 8 条「无法验证项」；
> 后者以**全新视角**复验合并方案，其结论是「**不支持**采信」——并因此抓出了 §4.1 所述的变异套件失效。
> **两份报告都没被「全绿」说服**，这正是它们有价值的原因。
