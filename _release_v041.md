# Decathlon China Digital 内部门户 · v0.4.1 发布说明

> 版本：v0.4.1（v0.4 的修订版）
> 日期：2026-09-19
> 上一版：v0.3（commit `48f473e`）
> 交付形态：`portal/dist/` 可直接双击 `file://` 打开

---

## 1. 为什么有 v0.4.1

v0.4 交付后用户判定「做的不够好」，给出三条明确指令：

> 1. 员工标签体系不用横纵两个轴，太复杂了，就建立统一的技能标签树以及成熟度就好，可以覆盖兼顾产品经理、研发工程师、QA工程师等各类digital岗位，即标签树不区分适配岗位
> 2. 个人主页：基础信息板块，员工技能标签以及成熟度呈现为主，近期知识贡献为辅，不用其他内容了
> 3. 页面布局更有个人主页的特征

三条指令的共同指向是**做减法**：v0.4.1 是简化版本，不是增强版本。

---

## 2. 指令 1：双轴 → 统一技能标签树

### 2.1 结构变化

| | v0.4 | v0.4.1 |
|---|---|---|
| 模型 | 领域轴（5 组）× 能力类型轴（4 组）正交 | 单棵技能树（7 组） |
| 组数 | 9 组（两条轴各自分组） | **7 组** |
| `axis` 字段 | `domain` / `capability`，驱动配色、字形、分区、筛选 | **已从数据中彻底删除** |

最终 7 个分组（岗位中立，组名不含任何岗位词）：

| 序 | 分组 | active 数 |
|---|---|---|
| 1 | AI 与算法 | 7 |
| 2 | 工程与架构 | 14 |
| 3 | 数据与分析 | 11 |
| 4 | 产品与设计 | 6 |
| 5 | 业务与场景 | 13 |
| 6 | 平台与安全 | 3 |
| 7 | 协作与流程 | 13 |
| | **合计** | **67** |

另含 deprecated 2 条（`d-ai-stack`、`c-fullstack`）、merged 3 条（`d-agent-framework`→`d-agent`、`c-metrics-design`→`d-metrics`、`d-replenish`→`d-supply`），**词表总计 72 条，与 v0.4 逐 id 一致，零丢失**。

### 2.2 为什么分组维度是「技能主题」而非「岗位」

用户要求「标签树不区分适配岗位」。若按岗位分组（PM 组 / 研发组 / QA 组），跨岗位共用的标签（如「需求管理」「跨部门协同」）无处安放，且会被读成「这是 PM 的标签，不是我的」——正好违背用户意图。

7 个组名已用 grep 确认**不含任何岗位词**，且 `mapToTags()` 只按 aliases 做词→标签映射，**没有新增任何「岗位→标签」映射**（那是变相双轴复活）。

### 2.3 视觉层同步拆除轴制

| 项 | v0.4 | v0.4.1 |
|---|---|---|
| 字形 | `AXIS_GLYPH = { domain: '◈', capability: '◇' }` | 两字形废弃 → **`PRIMARY_GLYPH = '◈'`，只作主标签标记** |
| active 默认态 | `◈ 标签名` / `◇ 标签名` | **无字形前缀**，纯文字 |
| 轴配色 | 领域轴蓝底 / 能力轴灰底 | **统一中性底**（`c.page` / `c.border` / `c.text2`） |
| 标签卡角标 | 「领域」/「能力」 | **所属分组名** |
| 面板头 | 「领域标签 N · 能力标签 M」 | **「N 组 · 共 M 个标签」** |

**默认态去字形的理由**：单树后每个标签都带同一符号 = 没有符号 = 纯噪音。字形只在 primary/selected 出现，语义重量反而提升。

**取消轴配色的理由**：原配色隐含「领域比能力重要」的价值判断（蓝主色 vs 灰退后）。单树后 7 组无高下之分，保留会造成用户继续追问「哪些算领域」。

### 2.4 连带面（易漏，本期已覆盖）

- `TagBrowse.jsx`：筛选器从「全部 / 领域轴 / 能力类型轴」三档 Segmented → **「全部 + 7 个分组名」**（语义升级，非简单删除）
- `GlobalSearch.jsx`：标签搜索结果的描述从「领域轴/能力类型轴 · 组名」→ **只显示分组名**
- `mock.js`：`DOMAIN_GROUPS` / `CAP_GROUPS` → **合并为单一 `SKILL_GROUPS`**
- `TagArrow` 死组件（硬编码 `axis="domain"`，全 src 零消费者）**已删除**

---

## 3. 指令 2：个人主页做减法

### 3.1 删除的两个区块

| 区块 | 处理 |
|---|---|
| P2④ 能力分布概览（「领域 N 个 · 能力 M 个 · 近 12 月实证 N 条」+ Sparkline） | **删除** |
| P2⑤ 协作触点（「负责的工具」） | **删除** |
| 底部「本页为只读视图…」独立注释行 | **保留但并入标签树面板**作一行脚注（承载「不作为绩效评价」合规声明，不宜丢） |

真实浏览器渲染验证：正文中「领域」出现 **0 次**、「能力」出现 **0 次**、`svg` 元素 **0 个**、「负责的工具」**不出现** —— 确认是**真删**，不是折叠或换壳。

### 3.2 页面现在只有三件事

1. **基础信息**（hero）
2. **技能标签 + 成熟度**（主体，占最大面积）
3. **近期知识贡献**（次要，窄栏）

### 3.3 连带清理（死字段）

删除上述区块后失去消费者的导出已一并清理：`stats`（`{groups,tags,recent}`）、`toolsOfPerson()`、`DOMAIN_GROUPS`/`CAP_GROUPS` 兼容别名。**`MaturityAxis` 未动**——成熟度双证据模型（自评圆点 + 实证轨道，含吹牛态/收敛态）是 v0.4 最有价值的诚实机制，用户矛头指向的是「两个轴」的心智负担，不是成熟度本身。

---

## 4. 指令 3：布局更像个人主页

### 4.1 新建 `.dp-g-profile`，不碰 `.dp-g-article`

`.dp-g-article`（`1fr / 232px`）是**文章详情页**的栅格，个人主页复用它正是「像文章不像主页」的根因。它同时被 `ArticleDetail.jsx:90` 共用，改动会波及文章页。

本期**新增** `.dp-g-profile { grid-template-columns: minmax(0, 1.85fr) minmax(0, 1fr); }`，栅格间隙 16px → 24px。`.dp-g-article` 定义**一个字符未改**（`git diff` 确认）。

### 4.2 版式层次

```
① Breadcrumb
② Hero（通栏）—— 头像 64px / 姓名 24px / 部门·角色 / 地点·邮箱 ＋ 主标签卡 ≤2（含 expanded 成熟度）
③ 主体两栏 —— 左 1.85fr：技能标签树（按 7 组多段渲染） ｜ 右 1fr：近期知识贡献
```

Hero 从「四块等权」中独立出来成为视觉焦点；主标签数从 v0.4 的 ≤3 收窄为 **≤2**（宽度增加后 3 个会挤）。

### 4.3 真实几何实测（headless Chrome + CDP，`file://`）

| 视口 | 主列 | 辅列 | 宽比 | 结论 |
|---|---|---|---|---|
| 1440×1500 | `left=112 w=774` | `left=910 w=418` | **1.852** | `grid-template-columns: 773.75px 418.25px` ✓ |
| 768×1700 | `left=16 w=736` | `left=16 w=736`（第二列 top 在第一列之下） | 1.000 | 单列塌缩 ✓ |

---

## 5. 一处非视觉的行为变更

`collectEvidence()` 中曾有一条结构性闸门：

```js
// v0.4
if (hit && TAG_BY_ID[hit] && TAG_BY_ID[hit].axis === 'domain') ids.add(hit);
// v0.4.1
if (hit && TAG_BY_ID[hit]) ids.add(hit);
```

原闸门导致**能力轴标签天然拿不到实证**。单树后闸门消失，实证覆盖范围扩大。

**实测影响**：分布**没有变化**——吴桐吹牛态仍 2 个、周敏吹牛双行仍 7 / 提示行 7 / 收轨 3、沈知微吹牛 0。原因是这些样本本就无证据命中。

唯一变化是**沈知微收轨淡文案 10 → 9**。归因（已修正）：**hero 主标签由 ≤3 收窄为 ≤2**，她有 3 个主标签，少渲染 1 处收敛文案。*（初版归因误记为「`c-metrics-design` 变 merged」，经独立审查官用 DOM 逐节点验证推翻——合并态标签卡仍照常渲染成熟度行。）*

---

## 6. 验证证据

三套测试均由主理人独立复跑，非采信施工方报告。

| 套件 | 结果 |
|---|---|
| `tests/smoke.cjs` | **156/156 通过，0 失败**（v0.3 基线 152，本期净增 4 条门） |
| `tests/shots.cjs` | 断言 1–4 全过；含 **1440/768 个人主页真实几何门**、110 路由抽点、1546×2 连续宽度扫描 |
| `tests/mutation.cjs` | **9/9 变异全部真红**，收尾 sha 回基线、工作树洁净 |

### 6.1 变异测试新增 3 条（本期重点）

v0.3 遗留的 6 个变异**全部指向 demand/Brd 代码**，v0.4.1 改动的标签树、个人主页、TagBrowse **零变异覆盖**——这是本项目第三次面临「名义覆盖 ≠ 实际覆盖」的风险（前两次：文本门冒充实测门；变异 `find` 字符串指向已迁移代码导致 0 命中却报成功）。

本期补齐：

| 变异 | 目标 | 变红断言数 |
|---|---|---|
| M7 | `mock.js` `SKILL_GROUPS` 分组名改坏 | 5 |
| M8 | `ui.jsx` 去掉 TagMatrix「只渲染有标签的分组」过滤 | 2 |
| M9 | `global.css` `.dp-g-profile` `1.85fr` → `1fr` | 1 |

三个 `find` 字符串均已独立核验**恰好命中 1 次**（非 0 命中的静默假绿）。目标文件 `TARGETS` 已加入 `ui.jsx`。

**M7 的一个诚实修订**：最初设想的「`SKILL_GROUPS` 7 组 → 8 组」是**弱变异**——追加空组会被 `.filter(b => b.list.length > 0)` 滤掉，根本不会变红。改用「改坏组名」后才真正打红 5 条断言。

### 6.2 新增一条真几何门（非文本门）

新增的「`.dp-g-profile` 栅格门」与「死代码门」都是**文本门**（正则扫 dist CSS / src 文本），只证明「字符串存在」，不证明「布局真的是 1.85:1」。因此 `shots.cjs` 新增**真实几何断言**：headless Chrome 打开 `file://`，读 `getBoundingClientRect()` 断言列宽比 ∈ [1.70, 2.05]，以及 ≤900px 单列塌缩。实测 **1.852**。

该门已做变异自证：临时把 `1.85fr` 改 `1fr` 重建 → 实测宽比 1.000 落到区间外，门确实变红。

### 6.3 离线交付四项约束（独立实测）

| 约束 | 结果 |
|---|---|
| IIFE 输出 | ✓ `(function(){"use strict";…` |
| 无 `type="module"` | ✓ HTML 与 JS 均 false |
| 无 `crossorigin` | ✓ HTML false；JS 0 次 |
| 零外部请求 | ✓ HTML/CSS 中 `http(s)://` 0 次；CDP `Network.enable` 拦截 `externalRequests=[]` |

---

## 7. 独立质量审查

由质量审查官以新鲜视角复核，**5 维评分 20/25**：

| 维度 | 分 | 依据 |
|---|---|---|
| 设计哲学 | 4/5 | 双轴→单树是真简化（9 组→7 组），不是改名 |
| 视觉层次 | 4/5 | hero 通栏 → 主栏 774px / 辅栏 418px，层次成立 |
| 执行质量 | 4/5 | 零 axis 残留、零死链、零控制台报错 |
| 特异性 | 4/5 | 成熟度双轨 / 吹牛态 / 合并审计是特有设计，非通用模板 |
| 克制 | 4/5 | 真做减法（删两块 + hero 收窄） |

**Anti-Slop P0 = 0**。审查官独立证伪过：手工变异栅格门确认会红、6+3 个变异 `find` 命中数全部为 1、真实浏览器几何与列宽比、CDP 网络拦截零外部请求。

审查提出的 3 个 P1 **已全部修复**（归因错误 / 3 个死字段 / 变异覆盖缺口 + 几何门）。

---

## 8. 变更文件清单

**源码（6）**

| 文件 | 改动 |
|---|---|
| `portal/src/data/mock.js` | TAG_DICT 按 7 组重写 group、删除 axis；2 条合并；`SKILL_GROUPS` 合并；实证闸门去除；排序改按组序；删 `stats`/`toolsOfPerson`/兼容别名 |
| `portal/src/components/ui.jsx` | 删 `AXIS_GLYPH` → `PRIMARY_GLYPH`；TagChip 六分支去轴化；TagMatrix 改 7 组多段树；删 `TagArrow` |
| `portal/src/pages/PersonProfile.jsx` | 重做版面：hero + 两栏；删能力分布概览 / 协作触点；主标签 ≤3→≤2 |
| `portal/src/pages/TagBrowse.jsx` | 轴筛选 → 分组筛选；`emptyCopy` 文案「领域」→「分组」 |
| `portal/src/components/GlobalSearch.jsx` | 标签搜索 desc 只显示分组名 |
| `portal/src/global.css` | 新增 `.dp-g-profile` + 900px 塌缩；`.dp-g-article` 零改动 |

**测试（3）**：`tests/smoke.cjs`（同步 6 处 + 新增 4 门）、`tests/shots.cjs`（新增真实几何门）、`tests/mutation.cjs`（新增 M7/M8/M9 + `TARGETS` 更新）

**构建产物**：`portal/dist/`

**规范文档（新增）**：`outputs/v041-requirements.md`、`outputs/v041-design-tokens.md`

**清理**：删除 4 个 v0.3 时期误入库的临时产物（`outputs/_fix-report-*.md`、`outputs/_verify-merged-v03.md`）

---

## 9. 已知项与遗留

1. **截图 PNG 存在像素级漂移**：`portal/shots/*.png` 每次运行生成的文件大小相同但字节不同。经逐 chunk 分析，差异在 IDAT 像素流（解压后长度相同、内容不同），属渲染时抗锯齿差异，**非内容变化**（如 `11-news-release-1440.png` 是 Release 页，与本期改动无关）。这些是测试副产物，像素不稳定，纳入版本控制会产生噪音。
2. **`d-` / `c-` id 前缀保留**：现已退化为纯历史 id，经 grep 确认**无任何逻辑按前缀分支**（`startsWith('d-')` / `'d-'` 字面量 0 命中），不构成第二轴复活。属可接受技术债。
3. **规范文档 §1.4 汇总表有误**：设计系统专家给出的 active 总数 70 与自身 §1.3 映射表对不上（`d-cloudnative` 重复登记、数据组计数矛盾）。实现采用「每个 id 恰好出现一次」口径得 **67**，并以「72 条 id 零丢失」为准。**67 是对的。**
4. **hero 主标签恰为 2 个时右侧略空**：宽视口下 hero 右半留白偏多，属可优化项。
5. **`git push` 未执行**：本次仅本地提交与打标签，未推送 `origin`。

---

## 10. 复现方式

```powershell
# 构建（在 portal/ 下）
npx vite build

# 测试（需 NODE_PATH 指向隔离依赖目录）
$env:NODE_PATH="C:\Users\uuzz\.workbuddy\binaries\node\workspace\node_modules"
node tests/smoke.cjs      # 156/156
node tests/shots.cjs      # 断言 1-4 + 几何门
node tests/mutation.cjs   # 9/9 变异真红
```

交付物：`portal/dist/index.html` —— **双击即可打开，无需服务器**。
